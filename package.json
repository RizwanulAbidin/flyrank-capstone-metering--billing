{
  "name": "flyrank-capstone-metering-billing",
  "version": "0.1.0",
  "description": "Usage metering and billing engine - idempotent metering, quota enforcement, money math, Stripe test mode",
  "main": "src/index.js",
  "scripts": {
    "test": "node --env-file=.env --test \"test/*.test.js\"",
    "migrate": "node --env-file=.env src/db/migrate.js",
    "seed": "node --env-file=.env src/db/seed.js",
    "start": "node --env-file=.env src/index.js",
    "reconcile": "node --env-file=.env src/jobs/reconcile.js",
    "seed:demo": "node --env-file=.env src/db/seedDemo.js"
  },
  "keywords": [],
  "author": "Rizwanul Abidin Asim",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.2.1",
    "pg": "^8.16.3",
    "stripe": "^22.5.0",
    "zod": "^4.4.3"
  }
}
